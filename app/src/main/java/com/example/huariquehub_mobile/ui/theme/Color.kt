package com.example.huariquehub_mobile.ui.theme

import androidx.compose.ui.graphics.Color

// --- Paleta principal (alineada con pointflavor, tono cálido, acento brasa) ---

// Acento principal — brasa (#C5481E)
val OrangePrimary    = Color(0xFFC5481E)
// Contenedor del acento — suave terracota para fondos de chips/cards
val OrangeLight      = Color(0xFFFAE8DF)
// Acento claro para modo oscuro
val OrangeDark       = Color(0xFFE8835A)

// Tinta profunda (ink) — marrón cálido oscuro
val BrownDark        = Color(0xFF382B1F)
// Tinta media (ink-2)
val BrownMedium      = Color(0xFF69584A)
// Contenedor cálido para modo oscuro
val BrownLight       = Color(0xFFFAEDE5)

// Crema cálida — extremo del gradiente en tarjetas
val WarmCream        = Color(0xFFF0E8DC)
// YellowGreen se reutiliza como crema cálida para mantener compatibilidad con screens existentes
val YellowGreen      = Color(0xFFF0E8DC)

// Fondo principal — blanco casi-cálido
val WarmWhite        = Color(0xFFFAF7F2)
// Fondo suavizado para secciones, chips, contenedores
val BackgroundSoft   = Color(0xFFF3EDE3)
// Superficie blanca pura (cards, dialogs)
val SurfaceColor     = Color(0xFFFFFFFF)

// Texto
val TextPrimary      = Color(0xFF382B1F)   // = BrownDark
val TextSecondary    = Color(0xFF69584A)   // = BrownMedium
val TextTertiary     = Color(0xFF9A8879)   // etiquetas, placeholders

// Separadores
val DividerWarm      = Color(0xFFE6DDD5)

// Estados
val StarYellow       = Color(0xFFFFC107)
val ErrorRed         = Color(0xFFB84028)

// Modo oscuro
val SurfaceDark      = Color(0xFF2A2018)
